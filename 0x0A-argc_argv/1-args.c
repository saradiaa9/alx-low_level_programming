#include "main.h"
#include <stdio.h>

/**
 * main1 - Entry point
 * @argc: The number of arguments passed to the program
 *
 * Return: Always 0 (success)
 */
int main1(int argc)
{
printf("%d\n", argc);

return (0);
}
